name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: 🚀 Checkout
      uses: actions/checkout@v4
      
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 📦 Install Flask
      run: pip install flask
      
    - name: 🏗️ Build site
      run: python generate_static.py
      
    - name: 🧹 Clean and deploy
      run: |
        # Удаляем ВСЕ файлы кроме .git
        find . -maxdepth 1 ! -name '.' ! -name '.git' -exec rm -rf {} +
        
        # Копируем только статический сайт из docs
        cp -r docs/* .
        
        # Удаляем папку docs
        rm -rf docs
        
        # Настраиваем git
        git config --global user.name "GitHub Actions"
        git config --global user.email "github-actions@github.com"
        
        # Коммитим и пушим
        git add .
        git commit -m "🚀 Deploy clean site" || exit 0
        git push